# =============================================================================
# ProcessPilot Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.local and configure for your environment
# 📖 Full documentation: docs/ENVIRONMENT_VARIABLES.md
# 🔧 Setup guide: docs/DEVELOPMENT_SETUP.md
# ℹ️  Frontend environment variables are prefixed with VITE_ and are public

# =============================================================================
# API CONFIGURATION
# =============================================================================

# Backend API base URL
# Development (with proxy): not needed (uses Vite proxy)
# Development (without proxy): http://localhost:5000/api
# Production: https://api.yourdomain.com/api
# VITE_API_URL=http://localhost:5000/api

# WebSocket connection URL for real-time features
# Development: http://localhost:5000
# Production: https://api.yourdomain.com
# VITE_WS_URL=http://localhost:5000

# =============================================================================
# BUILD & DEPLOYMENT CONFIGURATION
# =============================================================================

# Build environment (automatically set by Vite)
# Values: development | production
# Used by Vite internally - usually don't need to set manually
# VITE_NODE_ENV=development

# Application version (optional)
# Useful for displaying version in UI
# VITE_APP_VERSION=1.0.0

# API timeout in milliseconds (optional)
# Default: 10000 (10 seconds)
# VITE_API_TIMEOUT=10000

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================

# Enable debug mode for additional logging
# Development: true | Production: false
# VITE_DEBUG_MODE=false

# Enable experimental features
# VITE_ENABLE_EXPERIMENTAL_FEATURES=false

# Enable analytics/tracking (optional)
# VITE_ENABLE_ANALYTICS=false

# =============================================================================
# ENVIRONMENT-SPECIFIC EXAMPLES
# =============================================================================

# --- DEVELOPMENT (Local) ---
# Default Vite proxy handles API routing automatically
# Usually no environment variables needed for local development
# Vite proxy forwards /api/* to http://localhost:5000

# --- DEVELOPMENT (No Proxy) ---
# Uncomment if not using Vite proxy configuration
# VITE_API_URL=http://localhost:5000/api
# VITE_WS_URL=http://localhost:5000

# --- STAGING ---
# VITE_API_URL=https://staging-api.processpilot.com/api
# VITE_WS_URL=https://staging-api.processpilot.com
# VITE_APP_VERSION=1.0.0-staging

# --- PRODUCTION ---
# VITE_API_URL=https://api.processpilot.com/api
# VITE_WS_URL=https://api.processpilot.com
# VITE_APP_VERSION=1.0.0

# =============================================================================
# IMPORTANT NOTES
# =============================================================================

# 🔒 SECURITY WARNING:
#   All VITE_ prefixed variables are PUBLIC and included in the built bundle.
#   NEVER include sensitive data like API keys, secrets, or passwords.
#   These values are visible to end users in the browser.

# 📁 FILE NAMING:
#   .env.local - Local development overrides (gitignored)
#   .env.development - Development environment defaults
#   .env.production - Production environment defaults  
#   .env.example - This template file

# 🔄 LOADING ORDER (Vite):
#   1. .env.local (highest priority, gitignored)
#   2. .env.[NODE_ENV].local
#   3. .env.[NODE_ENV]
#   4. .env (lowest priority)

# 🚀 QUICK SETUP:
#   1. Copy this file: cp .env.example .env.local
#   2. Uncomment needed variables for your setup
#   3. Start dev server: npm run dev
#   4. API calls will use configured URLs or Vite proxy

# 📖 Documentation: docs/ENVIRONMENT_VARIABLES.md