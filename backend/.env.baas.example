# ProcessPilot - BaaS Provider Environment Examples
# Copy this file to .env and uncomment the section for your chosen provider

# =============================================================================
# COMMON CONFIGURATION (Required for all providers)
# =============================================================================

NODE_ENV=development
PORT=5000
HOST=localhost

# JWT Configuration (Required)
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-replace-this-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-refresh-token-secret-key-min-32-chars-replace-this
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# =============================================================================
# DATABASE PROVIDER SELECTION
# =============================================================================
# Set DB_PROVIDER to one of: postgresql, supabase, planetscale, neon, railway, generic
DB_PROVIDER=postgresql

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Uncomment for Supabase (recommended for quick setup)

# DB_PROVIDER=supabase
# SUPABASE_DB_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
# # OR use individual parameters:
# # SUPABASE_DB_HOST=db.[project-ref].supabase.co
# # SUPABASE_DB_PORT=5432
# # SUPABASE_DB_NAME=postgres
# # SUPABASE_DB_USER=postgres
# # SUPABASE_DB_PASSWORD=[your-password]
# DB_POOL_MIN=1
# DB_POOL_MAX=10

# =============================================================================
# STANDARD POSTGRESQL (Local or Hosted)
# =============================================================================
# Uncomment for local PostgreSQL or any PostgreSQL hosting

DB_PROVIDER=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=process_pilot
DB_USER=postgres
DB_PASSWORD=your_password
DB_SSL=false
DB_POOL_MIN=2
DB_POOL_MAX=10

# =============================================================================
# PLANETSCALE CONFIGURATION (MySQL)
# =============================================================================
# Uncomment for PlanetScale

# DB_PROVIDER=planetscale
# PLANETSCALE_DB_URL=mysql://[username]:[password]@[host]/[database]?ssl={"rejectUnauthorized":true}
# # OR use individual parameters:
# # PLANETSCALE_DB_HOST=aws.connect.psdb.cloud
# # PLANETSCALE_DB_PORT=3306
# # PLANETSCALE_DB_NAME=[database-name]
# # PLANETSCALE_DB_USER=[username]
# # PLANETSCALE_DB_PASSWORD=[password]
# DB_POOL_MIN=1
# DB_POOL_MAX=10

# =============================================================================
# NEON CONFIGURATION (Serverless PostgreSQL)
# =============================================================================
# Uncomment for Neon

# DB_PROVIDER=neon
# NEON_DB_URL=postgresql://[user]:[password]@[endpoint]/[database]?sslmode=require
# # OR use individual parameters:
# # NEON_DB_HOST=[endpoint]
# # NEON_DB_PORT=5432
# # NEON_DB_NAME=[database]
# # NEON_DB_USER=[user]
# # NEON_DB_PASSWORD=[password]
# DB_POOL_MIN=0
# DB_POOL_MAX=5

# =============================================================================
# RAILWAY CONFIGURATION
# =============================================================================
# Uncomment for Railway PostgreSQL

# DB_PROVIDER=railway
# RAILWAY_DB_URL=postgresql://postgres:[password]@[host]:[port]/railway
# # OR use individual parameters:
# # RAILWAY_DB_HOST=[host].railway.app
# # RAILWAY_DB_PORT=5432
# # RAILWAY_DB_NAME=railway
# # RAILWAY_DB_USER=postgres
# # RAILWAY_DB_PASSWORD=[password]
# DB_POOL_MIN=1
# DB_POOL_MAX=10

# =============================================================================
# GENERIC CONFIGURATION (Any PostgreSQL-compatible service)
# =============================================================================
# Uncomment for generic PostgreSQL service using connection string

# DB_PROVIDER=generic
# DATABASE_URL=postgresql://username:password@hostname:port/database
# DB_CLIENT=pg
# DB_TIMEOUT=30000
# DB_IDLE_TIMEOUT=600000
# DB_POOL_MIN=1
# DB_POOL_MAX=10

# =============================================================================
# OPTIONAL CONFIGURATION (All Providers)
# =============================================================================

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=noreply@processpilot.com

# Logging
LOG_LEVEL=info
LOG_FORMAT=combined
DEBUG_SQL=false

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# External Integrations (Future)
# REDIS_URL=redis://localhost:6379
# SLACK_WEBHOOK_URL=
# TEAMS_WEBHOOK_URL=

# =============================================================================
# PROVIDER-SPECIFIC SETUP NOTES
# =============================================================================

# SUPABASE SETUP:
# 1. Create account at supabase.com
# 2. Create new project
# 3. Get database URL from Settings > Database
# 4. Run migrations: npm run db:migrate
# 5. Run seeds: npm run db:seed

# PLANETSCALE SETUP:
# 1. Create account at planetscale.com
# 2. Create database and branch
# 3. Get connection string from dashboard
# 4. Install mysql2: npm install mysql2
# 5. Run migrations: npm run db:migrate

# NEON SETUP:
# 1. Create account at neon.tech
# 2. Create database
# 3. Get connection string from dashboard
# 4. Note: Serverless - use minimal pool settings

# RAILWAY SETUP:
# 1. Create account at railway.app
# 2. Deploy PostgreSQL service
# 3. Get connection details from service dashboard
# 4. Run migrations: npm run db:migrate