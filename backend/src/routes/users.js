const express = require('express');
const router = express.Router();

// Placeholder - will be implemented in next phase
router.get('/', (req, res) => {
  res.json({ message: 'Users endpoint - coming soon in Phase 2' });
});

module.exports = router;